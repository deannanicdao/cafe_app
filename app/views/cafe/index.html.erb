<h1>Welcome to the Cafe</h1>

<% @menu.inspect %>
<table>
    <tr>
        <th>Item</th>
        <th>Price</th>
    </tr>
    <% @menu.each do |key, value| %>
    <tr>
        <td><%= key.capitalize %></td>
        <td><%= "$%.2f" % value %></td>
    </tr>
    <% end %>       
    
</table>
